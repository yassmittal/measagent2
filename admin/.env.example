# Server-side only. The portal's own server calls the api; the browser never
# does, so nothing here is NEXT_PUBLIC and the admin token never reaches it.
MA_API_BASE_URL=http://127.0.0.1:3010
# Where avatar pages live, so a reviewer can open the one they are judging.
MA_WEB_BASE_URL=http://localhost:3000
