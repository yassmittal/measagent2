# @measagent/web

The Next.js frontend. Setup, environment and conventions are in the
[root README](../README.md); `../CLAUDE.md` has the working rules.

```bash
bun run dev        # :3000, expects the API on :3010
bun run lint:fix
bun run typecheck
bun run build
```
