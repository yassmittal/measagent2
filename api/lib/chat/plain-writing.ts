/**
 * How anything the model writes for people to read should sound. Shared by
 * avatar replies, the memory notes visitors can open, and the weekly email, so
 * nothing on the site reads like it was written by a machine.
 */
export const PLAIN_WRITING_RULES = `- Use plain, everyday words and short sentences, the way a person would.
- Never use em dashes or en dashes. Use a comma or a full stop instead.`;
